# Component tree
- App  
  - HomePage(pages)  
    - PhotoView(views)
      - PhotoList(templates)
        - PhotoListItem(parts)
      - PhotoFullScreenDialog(templates)
    - PhotoPost(views)
      - PhotoPostDialog(templates)
      - PhotoPostedDialog(templates)
  - DashBoardPage(pages)
    - ChartsView(views)